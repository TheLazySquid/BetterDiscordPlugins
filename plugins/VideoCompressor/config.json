{
    "$schema": "../schema.json",
    "description": "Compress videos that are too large to upload normally. Supports images as well.",
    "version": "0.5.1",
    "modules": [
        "attachFiles",
        "maxUploadSize",
        "adjustUploadSize",
        "modalMethods",
        "Modal"
    ]
}