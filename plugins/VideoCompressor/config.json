{
    "$schema": "../schema.json",
    "description": "Compress videos that are too large to upload normally",
    "version": "0.4.0",
    "modules": [
        "attachFiles",
        "maxUploadSize",
        "modalMethods",
        "Modal"
    ]
}